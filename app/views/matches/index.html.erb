<script type="text/javascript">
   var player_1_score = 0;
   var player_2_score = 0;
   var max = 0;
   check = true
  $(document).on("keydown", 'body', function(e){
    console.log("fskdjfh")
    var key = e.which || e.keyCode;
    if (key == 81){
      if (player_1_score > 20 && player_1_score > player_2_score+1 && key == 81){
        alert("You are not allowed to gain more points")
        check = false
      }else if(player_1_score > 20 && player_1_score > player_2_score+1){
        var result = prompt("Player 1 wins. To submit the scores enter: yes otherwise hit cancel")
          if (result == 'yes') {
            alert("Submitting results")
            $('.box1').html('<div><h1>Player One is the winner!</h1><a id="playagain">Play Again</a></div>')
            $('.box2').html('<div><h1>Sorry better luck next time!</h1></div>')
          }
        }
        else{
          player_1_score += 1
        }
        $('#player_1_score').html(player_1_score);
    }
    if (key == 65){
      player_1_score -= 1;
      $('#player_1_score').html(player_1_score);
    }
    if(player_1_score > 20 && player_1_score > player_2_score+1 && check == true){
      var result = prompt("Player 1 wins. To submit the scores enter: yes otherwise hit cancel")
    }
    if (key == 80){
      if (player_2_score > 20 && player_2_score > player_1_score+1 && key == 80){
        alert("You are not allowed to gain more points")
        check = false
      }
      else{
          player_2_score += 1
      }
      $('#player_2_score').html(player_2_score);
    }
    if (key == 76){
      player_2_score -= 1;
      $('#player_2_score').html(player_2_score);
    }
    if(player_2_score > 20 && player_2_score > player_1_score+1 && check == true){
      var result = prompt("Player 2 wins. To submit the scores enter: yes otherwise hit cancel")
    }
    if (player_2_score > 20 && player_2_score > player_1_score+1){
      alert("player 2 wins");
    }
  })
</script>
<div class="wrapper">
  <div class="box box1">
    <h1>User 1</h1>
    <h4 id="player_1_score">0</h4>
  </div>
  <div class="box box2" >
    <h3>User 2</h3>
    <h4 id="player_2_score">0</h4>
  </div>
</div>
